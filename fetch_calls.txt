   357	      const tokenRes = await fetch('https://api.simkl.com/oauth/token', {
   389	      const tokenRes = await fetch('https://myanimelist.net/v1/oauth2/token', {
   409	      const tokenRes = await fetch('https://anilist.co/api/v2/oauth/token', {
   624	            const simklRes = await fetch('https://api.simkl.com/users/settings', {
   647	            const malRes = await fetch('https://api.myanimelist.net/v2/users/@me', {
   673	            const anilistRes = await fetch('https://graphql.anilist.co', {
  1998	      const res = await fetch(`https://api.github.com/gists${targetId ? '/' + targetId : ''}`, {
  2030	      const getRes = await fetch(`https://api.github.com/repos/${owner}/${repo}/contents/${path}`, {
  2041	      const res = await fetch(`https://api.github.com/repos/${owner}/${repo}/contents/${path}`, {
  2087	      const res = await fetch(url, {
  2107	        const res = await fetch(putUrl, {
  2150	      const r = await fetch(`https://api.github.com/gists/${targetId}`, { headers: { 'Authorization': `Bearer ${token}` } });
  2160	      const r = await fetch(`https://api.github.com/repos/${owner}/${repo}/contents/${path}`, { headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/vnd.github.v3.raw' } });
  2165	       const r = await fetch(`https://www.googleapis.com/drive/v3/files/${targetId}?alt=media`, { headers: { 'Authorization': `Bearer ${token}` } });
  2170	       const r = await fetch(fetchUrl, { headers: { 'Authorization': `Bearer ${token}` } });
  2440	    const response = await fetch(`${appSettings.remoteSync.serverUrl}/api/remote-sync/receive`, {
  2465	    const response = await fetch(`${appSettings.remoteSync.serverUrl}/api/remote-sync/export`, {
